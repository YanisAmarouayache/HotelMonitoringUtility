{
  "name": "hotel-monitoring-utility",
  "version": "1.0.0",
  "description": "Hotel monitoring utility with scraper, backend and frontend",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd apps/backend && npm run dev",
    "dev:frontend": "cd apps/frontend && npm run dev",
    "dev:scraper": "cd packages/scraper && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd apps/backend && npm run build",
    "build:frontend": "cd apps/frontend && npm run build",
    "install:all": "npm install && cd apps/backend && npm install && cd ../../apps/frontend && npm install && cd ../../packages/scraper && npm install",
    "setup": "npm run install:all && cd apps/backend && npm run db:setup"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 